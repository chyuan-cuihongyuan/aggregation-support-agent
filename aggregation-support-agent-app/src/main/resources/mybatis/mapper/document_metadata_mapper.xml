<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.chyuan.ai.infrastructure.persistent.mapper.DocumentMetadataMapper">

    <resultMap id="DocumentMetadataMap" type="cn.chyuan.ai.infrastructure.dao.po.DocumentMetadataPO">
        <id column="id" property="id"/>
        <result column="document_id" property="documentId"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="owner_user_id" property="ownerUserId"/>
        <result column="file_name" property="fileName"/>
        <result column="file_extension" property="fileExtension"/>
        <result column="file_size" property="fileSize"/>
        <result column="mime_type" property="mimeType"/>
        <result column="total_chars" property="totalChars"/>
        <result column="total_chunks" property="totalChunks"/>
        <result column="section_count" property="sectionCount"/>
        <result column="processing_status" property="processingStatus"/>
        <result column="error_message" property="errorMessage"/>
        <result column="visibility" property="visibility"/>
        <result column="deleted_flag" property="deletedFlag"/>
        <result column="user_id" property="userId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="cn.chyuan.ai.infrastructure.dao.po.DocumentMetadataPO"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO document_metadata (document_id, tenant_id, owner_user_id, file_name, file_extension, file_size, mime_type,
                                       total_chars, total_chunks, section_count, processing_status,
                                       error_message, visibility, deleted_flag, user_id, create_time, update_time)
        VALUES (#{documentId}, #{tenantId}, #{ownerUserId}, #{fileName}, #{fileExtension}, #{fileSize}, #{mimeType},
                #{totalChars}, #{totalChunks}, #{sectionCount}, #{processingStatus},
                #{errorMessage}, #{visibility}, #{deletedFlag}, #{userId}, #{createTime}, #{updateTime})
    </insert>

    <select id="queryByScope" resultMap="DocumentMetadataMap">
        SELECT * FROM document_metadata
        WHERE deleted_flag = 0
          AND tenant_id = #{scope.tenantId}
          AND owner_user_id = #{scope.ownerUserId}
        ORDER BY create_time DESC
    </select>

    <!-- 仅管理员可用：不带租户隔离，普通业务请使用 queryByDocumentIdAndScope -->
    <select id="adminQueryByDocumentId" resultMap="DocumentMetadataMap">
        SELECT * FROM document_metadata
        WHERE document_id = #{documentId}
          AND deleted_flag = 0
    </select>

    <select id="queryByDocumentIdAndScope" resultMap="DocumentMetadataMap">
        SELECT * FROM document_metadata
        WHERE document_id = #{documentId}
          AND deleted_flag = 0
          AND tenant_id = #{scope.tenantId}
          AND owner_user_id = #{scope.ownerUserId}
    </select>

    <update id="updateStatus">
        UPDATE document_metadata
        SET processing_status = #{processingStatus},
            total_chunks      = #{totalChunks},
            total_chars       = #{totalChars},
            section_count     = #{sectionCount},
            error_message     = #{errorMessage},
            update_time       = NOW()
        WHERE document_id = #{documentId}
          AND tenant_id = #{scope.tenantId}
    </update>

    <update id="markDeletedByDocumentId">
        UPDATE document_metadata
        SET deleted_flag = 1,
            update_time = NOW()
        WHERE document_id = #{documentId}
          AND tenant_id = #{scope.tenantId}
          AND owner_user_id = #{scope.ownerUserId}
          AND deleted_flag = 0
    </update>

</mapper>
